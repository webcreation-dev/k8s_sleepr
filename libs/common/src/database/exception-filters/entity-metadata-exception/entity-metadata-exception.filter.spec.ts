import { EntityMetadataExceptionFilter } from './entity-metadata-exception.filter';

describe('EntityMetadataExceptionFilter', () => {
  it('should be defined', () => {
    expect(new EntityMetadataExceptionFilter()).toBeDefined();
  });
});
